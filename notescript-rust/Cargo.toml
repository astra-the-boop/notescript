[package]
name = "notescript-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
pyo3 = {version="0.21", features = ["serde", "auto-initialize"]}
serde_json = "1.0"
serde = {version = "1.0", features = ["derive"]}